# Exercise 2: Update By ID

Create an `updateProjectForm` component that:

- Accepts a project object
- Uses `UPDATE_PROJECT` mutation
- Lets the user change the name & description
- Displays a success message after update
